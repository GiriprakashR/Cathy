#!/usr/bin/env python
"""Cathy.

Discord chat bot using AIML artificial intelligence

Usage:
  alice <channel> <token> <logfile> <database>

Options:
  <channel>     Name of channel to chat in. E.g. "general"
  <token>       Bot's Discord API token
  <logfile>     Path to log file. E.g. "$HOME/alice.log"
  <database>    Path to database file. E.g. "$HOME/alice.sqlite"
  -h --help     Show this screen.
"""

from docopt import docopt
from alice.alice import Alice

args = docopt(__doc__)

print('Channel: %s' % args['<channel>'])
print('Token: %s' % args['<token>'])
print('Log file: %s' % args['<logfile>'])
print('Database: %s' % args['<database>'])

bot = Alice(args['<channel>'], args['<token>'], args['<logfile>'], args['<database>'])
bot.run()
